{% extends '::base.html.twig' %}

{%- block meta_title -%}
    {{ 'contact.meta_title'|trans({}, 'kisaan_meta') ~ " - " ~ kisaan_site_name }}
{%- endblock -%}

{%- block meta_description -%}
    {{ 'contact.meta_desc'|trans({}, 'kisaan_meta') }}
{%- endblock -%}

{% form_theme form 'KisaanContactBundle:Form:fields.html.twig' %}

{% trans_default_domain 'kisaan_contact' %}

{% block layout %}

    {% embed '@KisaanCore/Frontend/layout.html.twig' %}

        {% block main %}

            {% include 'KisaanCoreBundle:Frontend/Common:_flash.html.twig' only %}

            <div id="form-contact" class="form-contact validate-form">
                <h1>{{ 'contact.new.form.title'|trans({},'kisaan_contact') }}</h1>

                {{ form_start(form) }}

                {{ form_widget(form) }}

                <div class="field-row">
                    <button type="submit" class="btn btn-primary right">
                        {{ 'contact.form.submit.button'|trans({},'kisaan_contact') }}
                    </button>
                </div>

                {{ form_end(form) }}
            </div>

        {% endblock %}

    {% endembed %}
{% endblock layout %}
